# Sample footage

Put 1-2 legal sample clips here for the demo (not committed to git).

Example names:
- `junction_sample.mp4`
- `checkpoint_sample.mp4`

Then set a camera stream to the file path, e.g.
`backend/video/samples/junction_sample.mp4`.

No clips yet? Use the simulator instead — the app works either way.
